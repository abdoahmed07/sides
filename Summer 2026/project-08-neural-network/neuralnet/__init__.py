# neuralnet package — neural network implemented from scratch using only NumPy
# I wanted to understand backpropagation at the actual math level, not just call fit()
# Everything here is hand-coded: layer math, activation functions, gradient computation
